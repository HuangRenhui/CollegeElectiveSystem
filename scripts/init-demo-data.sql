-- =====================================================================
-- 演示数据单独导入脚本
--
-- 【用途】
--   仅导入演示数据（选课记录、成绩、操作日志），不重建表结构。
--   适用于已有表结构、只想补充演示数据的场景。
--
-- 【使用前提】
--   数据库中已存在本系统的 13 张表（应用启动时会自动创建），
--   且已执行 data.sql 导入基础数据（院系、专业、用户、课程等）。
--
-- 【使用方式】
--   mysql -uroot -p <目标库> < scripts/init-demo-data.sql
--
--   或在项目根目录执行：
--   mysql -uroot -p college_elective < scripts/init-demo-data.sql
--
-- 【说明】
--   若要一次性完成「建表 + 基础数据 + 演示数据」，请改用 init-db.sql。
--   本脚本可重复执行，会在导入前清理已有的选课记录、成绩与操作日志。
-- =====================================================================

SOURCE ../backend/src/main/resources/db/demo-data.sql;
