# 待实现功能清单

> 本文档记录项目中**接口契约已定义、具体实现待补充**的功能，供后续开发时查阅。
>
> 当前状态：**选课模块**与**成绩模块**的 Service 实现类尚未编写，接口签名、
> 数据模型与基础设施（Redis 脚本、Mapper、工具类）均已就绪，可直接基于本清单实现。
> 两个模块的 Controller、前端页面与 API 封装均已完整编写，完成后端实现后即可直接联调。

## 目录

- [1. 总体说明](#1-总体说明)
- [2. 选课模块](#2-选课模块)
  - [2.5 前端时间依赖问题（待优化）](#25-前端时间依赖问题待优化)
  - [2.6 教务代选（管理员）](#26-教务代选管理员)
- [3. 成绩模块](#3-成绩模块)
- [4. 教学评价模块](#4-教学评价模块)
- [5. 已就绪的基础设施](#5-已就绪的基础设施)
- [6. 实现顺序建议](#6-实现顺序建议)
- [7. 验收清单](#7-验收清单)

---

## 1. 总体说明

### 1.1 当前工程状态

| 模块 | Controller | 前端页面 | Service 接口 | Service 实现 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 认证与个人中心 | ✅ | ✅ | ✅ | ✅ | 可用 |
| 课程管理 | ✅ | ✅ | ✅ | ✅ | 除缓存预热/人数同步外可用 |
| **选课** | ✅ 待补充方法体 | ✅ | ✅ | ⏳ 待编写 | **待实现** |
| **成绩** | ✅ 待补充方法体 | ✅ | ✅ | ⏳ 待编写 | **待实现** |
| **教学评价** | ✅ 已完成 | ✅ 已完成 | ✅ 已完成 | ✅ 已完成 | **✅ 可用**（见第 4 节） |
| 学生管理 | ✅ | ✅ | ✅ | ✅ | 可用 |
| 教师管理 | ✅ | ✅ | ✅ | ✅ | 可用 |
| 基础信息 | ✅ | ✅ | ✅ | ✅ | 可用 |
| 学期管理 | ✅ | ✅ | ✅ | ✅ | 可用 |
| 公告管理 | ✅ | ✅ | ✅ | ✅ | 可用 |
| 系统管理/日志 | ✅ | ✅ | ✅ | ✅ | 可用 |

### 1.2 待编写的内容

| 内容 | 位置 | 说明 |
| --- | --- | --- |
| 选课服务实现 | `service/impl/CourseSelectionServiceImpl.java` | **待新建**，实现 `CourseSelectionService` 的 8 个方法 |
| 成绩服务实现 | `service/impl/CourseGradeServiceImpl.java` | **待新建**，实现 `CourseGradeService` 的 6 个方法 |
| 教学评价实现 | `entity/CourseReview.java` 等 | **✅ 已完成**，详见 [4. 教学评价模块](#4-教学评价模块) |
| Controller 注入方式 | `StudentCourseController` 等 4 个类 | 实现完成后将 `ObjectProvider` 改为直接注入 |
| 定时任务开关 | `task/SelectionSyncTask#TASK_ENABLED` | 实现完成后置为 `true` |
| 成绩分布统计 | `CourseServiceImpl#getStatistics` | 实现完成后恢复 `scoreDistribution` 赋值 |

### 1.3 已就绪的内容

- ✅ **实体类**：`CourseSelection`、`CourseGrade`（含非数据库展示字段）
- ✅ **Mapper 接口**：`CourseSelectionMapper`、`CourseGradeMapper`（所有方法签名已定义）
- ✅ **Mapper XML**：`CourseSelectionMapper.xml`、`CourseGradeMapper.xml`（SQL 已编写完成）
- ✅ **DTO/VO**：`GradeInputDTO`、`SelectionResultVO`、`ConflictVO`、`GradeReportVO`
- ✅ **Lua 脚本**：`select_course.lua`、`drop_course.lua` 及 `RedisConfig` 中的 Bean 注册
- ✅ **常量与 Redis Key**：`Constants`、`RedisKeys` 中的全部相关定义
- ✅ **状态码枚举**：`ResultCode` 中 3001-3012（选课）、4001-4004（成绩）
- ✅ **数据库表与初始化数据**：`course_selection`、`course_grade` 表及示例数据
- ✅ **Controller**：完整的方法签名、`@Operation` 注解与 `@OperationLog` 埋点
- ✅ **前端页面**：学生端选课/选课记录/课表/成绩、教师端工作台/名单/成绩录入/课表
- ✅ **前端 API 层**：`api/student.js`、`api/teacher.js` 中的接口封装

### 1.4 运行期行为说明

**应用可正常启动运行**：Controller 通过 `ObjectProvider<XxxService>` 延迟获取 Service，
因此在其实现类缺失时不会导致 Spring 容器启动失败。

调用未实现接口时，返回统一业务提示（HTTP 200，业务码 `5006`）：

```json
{
  "code": 5006,
  "message": "学生选课 尚未实现，请参考 docs/待实现功能.md",
  "success": false
}
```

相关工具类：`com.college.elective.common.PendingFeature`，提供两种用法：

```java
// 方式一：返回「尚未实现」响应
CourseSelectionService service = selectionServiceProvider.getIfAvailable();
if (service == null) {
    return PendingFeature.result("学生选课");
}
return Result.success(service.selectCourse(courseId));

// 方式二：抛出「尚未实现」业务异常
CourseSelectionService service = PendingFeature.require(selectionServiceProvider, "学生选课");
```

### 1.5 实现完成后的替换方式

将 Controller 中的 `getIfAvailable()` 判空分支移除，直接注入 Service 并调用即可：

```java
// 改造前
private final ObjectProvider<CourseSelectionService> selectionServiceProvider;

// 改造后
private final CourseSelectionService selectionService;
```

---

## 2. 选课模块

### 2.1 需要实现的方法

在 `service/impl/CourseSelectionServiceImpl.java` 中实现以下方法：

| # | 方法 | 优先级 | 复杂度 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | `selectCourse(Long courseId)` | P0 | 高 | 待实现 |
| 2 | `dropCourse(Long courseId)` | P0 | 中 | 待实现 |
| 3 | `checkConflict(Long courseId)` | P0 | 中 | **已实现** |
| 4 | `pageAvailableCourses(CourseQueryDTO, Long)` | P0 | 低 | 待实现 |
| 5 | `pageMySelections(...)` | P1 | 低 | **已实现** |
| 6 | `pageCourseStudents(...)` | P1 | 低 | **已实现** |
| 7 | `preloadCourseCache(Long semesterId)` | P1 | 低 | 待实现 |
| 8 | `syncSelectionCount()` | P2 | 中 | 待实现 |
| 9 | `pageStudentSelections(Long studentId)` | P1 | 低 | 待实现 |
| 10 | `adminAddSelection(Long studentId, Long courseId, String reason)` | P1 | 中 | 待实现 |
| 11 | `adminRemoveSelection(Long studentId, Long courseId, String reason)` | P1 | 低 | 待实现 |

> 第 9-11 项为**教务代选**相关方法，详见 [2.6 教务代选](#26-教务代选管理员)。

**已实现方法说明**（第 3、5、6 项）：

- `checkConflict` —— 时间冲突预检，无副作用，供选课前提示使用：
  1. 通过 `SecurityUtils.requireStudentId()` 取当前学生，学生ID取自登录态，
     不接受请求参数，避免越权查询他人课表；
  2. 取当前学期并**判空**：`selectByStudentSelection` 的 SQL 中
     `cs.semester_id = #{semesterId}` 为硬编码条件，若传入 `null` 将查不到任何数据，
     导致冲突检测静默失效，故此处抛出 `CURRENT_SEMESTER_NOT_SET(5005)`；
  3. 分别查询目标课程排课与本人本学期已选课程排课（SQL 已限定 `status = 1`）；
  4. 两两比对，**跳过课程ID相同的记录**（学生已选该课时两侧会同时出现，自己与自己必然判定冲突）；
  5. 命中冲突时构造 `ConflictVO`，时间信息由 `sectionText`（如「第1-2节」）与
     `weekText`（如「第1-16周」）两个字段分别承载。

- `pageMySelections` —— 通过 `SecurityUtils.requireStudentId()` 取当前学生，调用
  `CourseSelectionMapper#selectStudentSelections` 分页查询。
- `pageCourseStudents` —— 先经 `checkCourseAccess` 校验访问权限（管理员不受限，
  教师仅能查看自己授课的课程，越权返回 403），再调用
  `CourseSelectionMapper#selectCourseStudents` 分页查询。

> 这三个方法属纯查询与计算，无并发与事务要求，已按标准增删查改实现。

**冲突判定算法**：已提取至 `common/ScheduleConflictUtils`，供选课预检
（本方法）与排课校验（`CourseServiceImpl#validateScheduleConflict`）共用，
避免两处分别实现导致行为不一致。该工具类按**星期、节次、周次、周类型**四个维度判定，
其中周类型规则为：任一方为 `ALL` 或两者类型相同则周次相交即冲突；
`ODD` 与 `EVEN` 需在公共周次区间内存在同奇偶的周次才算冲突
（例如 ODD 第 1-8 周与 EVEN 第 5-12 周在第 6、8 周重叠，仍判定冲突）。

### 2.2 涉及的业务规则

#### 规则 1：选课开关校验

需依次校验四处，任一不通过即拒绝选课：

| 校验项 | 数据来源 | 失败状态码 |
| --- | --- | --- |
| 全局配置开关 | `elective.selection.enabled`（配置类 `ElectiveProperties`） | 3007 |
| 动态开关 | Redis `elective:selection:switch`（值为 `off` 时关闭） | 3007 |
| 学期状态 | 当前学期 `semester.status`（见下方说明） | 3006 |
| 选课时间窗口 | 当前学期 `semester.select_start_time / select_end_time` | 3006 |

> 当前学期通过 `semester.is_current = 1` 查询。

**关于 `status` 字段**：`Semester#status` 取值为 `0-未开始 / 1-选课中 / 2-进行中 / 3-已结束`。
选课仅允许在 `status = 1` 时进行，建议与时间窗口校验**取交集**，即两者都通过才放行：

```java
// semester.status = 1 且 时间窗口内
if (semester.getStatus() == null || semester.getStatus() != 1) {
    throw new BusinessException(ResultCode.SELECTION_NOT_OPEN, "当前学期未开放选课");
}
if (!semester.inSelectionPeriod()) {
    throw new BusinessException(ResultCode.SELECTION_NOT_OPEN, "当前不在选课时间段内");
}
```

**可直接复用的现成方法**：`Semester#inSelectionPeriod()`（`entity/Semester.java:58`）已实现完整的时间窗口判定：

```java
public boolean inSelectionPeriod() {
    LocalDateTime now = LocalDateTime.now();
    return selectStartTime != null && selectEndTime != null
            && !now.isBefore(selectStartTime) && !now.isAfter(selectEndTime);
}
```

无需重复编写时间比较逻辑，直接调用即可。

> **注意**：`status` 由管理员在学期管理页手工维护，`inSelectionPeriod()` 依据时间自动判定，
> 两者可能不一致（例如管理员忘记改 `status`，但时间已到）。当前设计采用**交集**（都满足才可选课），
> 若希望「时间到了就自动开放」，需改为**并集**或由定时任务同步 `status`，实现时请明确取舍。

#### 规则 2：课程状态校验

- 课程不存在或已逻辑删除 → `3001`
- `course.status != 1`（下架或已结课）→ `3001`

#### 规则 3：学分上限校验

```
本学期已选学分（sumSelectedCredit）+ 本次课程学分 > elective.selection.max-credit
    → 抛出 3008，提示「选课后总学分将达到 X，超过本学期上限 Y 学分」
```

已选学分通过 `CourseSelectionMapper.sumSelectedCredit(studentId, semesterId)` 计算。

#### 规则 4：时间冲突检测（核心）

**四维判定**，四条全部满足才判定为冲突：

| 维度 | 判定条件 |
| --- | --- |
| 星期 | `dayOfWeek` 相同 |
| 节次 | `startA <= endB && endA >= startB` |
| 周次 | `[startWeekA, endWeekA]` 与 `[startWeekB, endWeekB]` 有交集 |
| 周类型 | 见下方说明 |

**周类型兼容规则**：

- 一方为 `ALL`（每周）→ 冲突
- 双方类型相同（都是 `ODD` 或都是 `EVEN`）→ 冲突
- 一方 `ODD` 一方 `EVEN` → 仅当存在**同为奇/偶数**的公共周次时冲突（遍历公共周次区间判断）

**数据获取**：

```java
// 目标课程的排课
List<CourseSchedule> targetSchedules = scheduleMapper.selectByCourseId(courseId);

// 该学生本学期已选课程的排课
List<CourseSchedule> selectedSchedules =
        scheduleMapper.selectByStudentSelection(studentId, semesterId);
```

**返回结构**：每个冲突构造成 `ConflictVO`，包含冲突课程名、节次描述、周次描述。

> 📌 **算法参考**：`src/test/java/com/college/elective/ScheduleConflictTests.java`
> 中已有 7 个测试用例覆盖边界场景（同天重叠、周次不重叠、单双周互斥等），
> 其 `isConflict` 方法可直接作为实现参照。

#### 规则 5：Redis 原子预占（核心）

**步骤**：

1. 计算 Redis Key：
   ```java
   String capacityKey = RedisKeys.COURSE_CAPACITY + courseId;  // elective:course:capacity:{id}
   String selectedKey = RedisKeys.COURSE_SELECTED + courseId;  // elective:course:selected:{id}
   ```
2. **确保缓存就绪**：若 `capacityKey` 不存在，需从数据库回源重建（建议加锁 + 双重检查）
3. **执行 Lua 脚本**：
   ```java
   Long result = stringRedisTemplate.execute(
           selectCourseScript,                       // RedisConfig 已注册的 Bean
           List.of(capacityKey, selectedKey),
           String.valueOf(studentId));
   ```

**Lua 返回值语义**（已定义在 `select_course.lua`）：

| 返回值 | 含义 | 对应处理 |
| --- | --- | --- |
| `0` | 预占成功 | 继续落库 |
| `1` | 已选过该课程 | 抛出 3003 |
| `2` | 余量不足 | 抛出 3002 |
| `3` | 缓存未初始化 | 抛出 3010 |

#### 规则 6：落库与回滚

```
预占成功
    ↓
写入选课记录（事务）
    ├─ 已有记录且 status=1  → 抛出 3003
    ├─ 已有记录且 status=0  → 复用记录，置 status=1、更新 selectTime、清空 dropTime
    └─ 无记录               → 新增记录，status=1、selectType=1
    ↓
成功 → courseMapper.increaseSelectedCount(courseId)
失败 → 回滚 Redis 预占（执行 drop_course.lua，归还余量 + 移除已选标记）
```

> ⚠️ **重要**：由于事务方法需通过代理调用，建议在 Service 中注入
> `ObjectProvider<CourseSelectionServiceImpl>` 获取自身代理，或将落库逻辑抽到独立 Bean，
> 避免 `this.persistSelection()` 自调用导致 `@Transactional` 失效。

#### 规则 7：退课

**前置校验**：

| 校验项 | 失败状态码 |
| --- | --- |
| 课程 `selectable != 1`（不允许退选） | 3009 |
| 未找到 `status = 1` 的选课记录 | 3004 |
| 该选课记录 `score != null`（已录入成绩） | 3009 |

**执行步骤**：

1. 更新选课记录：`status = 0`、`dropTime = now()`
2. 执行 `drop_course.lua` 归还 Redis 余量
3. `courseMapper.decreaseSelectedCount(courseId)`

> 注意：退课**不删除记录**，便于审计与重新选课复用。

#### 规则 8：缓存预热与同步

**`preloadCourseCache(semesterId)`**：

```java
// 1. 查询正常状态课程
List<Course> courses = courseMapper.selectList(...status = 1...);

// 2. 逐门课程重建缓存
for (Course course : courses) {
    // 查询真实已选学生
    List<CourseSelection> selections = list(...courseId, status = 1...);

    // 写入余量（String）
    int remaining = Math.max(maxCapacity - selections.size(), 0);
    stringRedisTemplate.opsForValue().set(capacityKey, String.valueOf(remaining),
            RedisKeys.DEFAULT_CACHE_MINUTES, TimeUnit.MINUTES);

    // 写入已选集合（Set）
    stringRedisTemplate.delete(selectedKey);
    stringRedisTemplate.opsForSet().add(selectedKey, studentIdArray);
}
```

**`syncSelectionCount()`**：

```java
// 对每门课程：比对数据库真实选课数 与 course.selected_count
// 不一致则：
//   1. courseMapper.syncSelectedCount(courseId)   // XML 中已实现该 SQL
//   2. 用真实值重算并覆盖 Redis 余量
```

### 2.3 接口与状态码对照

| 接口 | 路径 | 方法 | 可能返回的状态码 | 状态 |
| --- | --- | --- | --- | --- |
| 可选课程列表 | `GET /student/courses` | `pageAvailableCourses` | - | 待实现 |
| 选课 | `POST /student/courses/{id}/select` | `selectCourse` | 3001/3002/3003/3005/3006/3007/3008/3010 | 待实现 |
| 退课 | `POST /student/courses/{id}/drop` | `dropCourse` | 3001/3004/3009 | 待实现 |
| 冲突预检 | `GET /student/courses/{id}/conflict` | `checkConflict` | 5005 | **已实现** |
| 我的选课 | `GET /student/selections` | `pageMySelections` | - | **已实现** |
| 课程学生名单 | `GET /teacher/courses/{id}/students` | `pageCourseStudents` | 403 | **已实现** |
| 课程名单列表 | `GET /teacher/courses/{id}/selections` | `listCourseStudents`（课程服务） | 403 | **已实现** |
| 缓存预热 | `POST /admin/courses/cache/preload` | `preloadCourseCache` | - |
| 人数同步 | `POST /admin/courses/selection/sync` | `syncSelectionCount` | - |
| 学生选课记录（管理） | `GET /admin/students/{studentId}/selections` | `pageStudentSelections` | - |
| 教务代选 | `POST /admin/students/{studentId}/selections` | `adminAddSelection` | 3001/3002/3003/3005/3008 |
| 教务代退选 | `DELETE /admin/students/{studentId}/selections/{courseId}` | `adminRemoveSelection` | 3001/3004/3009 |

### 2.4 所需依赖注入

实现类建议注入以下依赖：

```java
private final CourseMapper courseMapper;
private final StudentMapper studentMapper;
private final SemesterMapper semesterMapper;
private final CourseScheduleMapper scheduleMapper;
private final StringRedisTemplate stringRedisTemplate;
private final DefaultRedisScript<Long> selectCourseScript;   // RedisConfig 已注册
private final DefaultRedisScript<Long> dropCourseScript;     // RedisConfig 已注册
private final ElectiveProperties properties;                 // 配置类
```

### 2.5 前端时间依赖问题（待优化）

**现状**：学生端选课倒计时组件 `frontend/src/utils/useSelectionCountdown.js` 直接使用浏览器本地时间
（`new Date()`）解析 `semester.selectStartTime / selectEndTime`，因此倒计时的准确性**依赖学生本机系统时间**。

**存在的问题**：

| 风险 | 影响 |
| --- | --- |
| 学生手动修改本机时间 | 倒计时显示失真，可能提前显示「已开放」或延迟显示 |
| 学生电脑时间未同步（时区/漂移） | 同上 |
| 跨时区访问 | 解析结果与服务器不一致 |

**安全说明**：此问题**仅影响前端展示与按钮可用状态，不构成安全漏洞**。
只要后端按「规则 1」正确校验 `status` 与 `inSelectionPeriod()`，即使前端被绕过，选课请求依然会被拒绝。
因此该问题属于**体验/公平性优化**，非阻塞性缺陷。

**建议的改进方案**（实现时二选一）：

**方案 A：后端下发服务器时间（推荐，改动小）**

1. 在 `Result` 或响应头中附带服务器当前时间戳；
2. 前端首次请求时记录 `serverTime` 与 `localTime` 的差值 `offset`；
3. 倒计时改用 `Date.now() + offset` 作为"当前时间"。

```js
// 前端伪代码
const offset = serverTimestamp - Date.now()
const currentTime = Date.now() + offset   // 替代 new Date()
```

**方案 B：后端直接下发剩余毫秒数**

在学期查询接口中额外返回 `selectionCountdownMs`（距离选课开始或结束的剩余毫秒），
由服务端计算，前端仅做递减展示。此方案前端无需关心时间解析，但需新增接口字段。

**建议实现时机**：与选课模块后端实现一并完成，避免前端二次改动。

### 2.6 教务代选（管理员）

#### 2.6.1 业务背景

代选功能用于处理学生**通过正常渠道无法完成选课**的特殊情况。参照高校教务的实际做法：

| 场景 | 实际处理方 | 真实做法 |
| --- | --- | --- |
| 学生错过选课时间 | 学生本人 → 教务员 | 学生提交申请，**教务员代选** |
| 必修课名单缺失 | 教务系统自动 | 系统按专业自动导入，不依赖人工 |
| 重修 / 插班 | 教务处 | 统一处理，单独走重修流程 |

> **教师端不提供代选能力**。教师对本课程学生的权限仅限：查看名单、导出名单、录入成绩。
> 这与高校实际一致——教师直接改名单会绕过学分、冲突、容量等校验，属于教学事故风险。

#### 2.6.2 接口设计

| 接口 | 路径 | 方法 | 说明 |
| --- | --- | --- | --- |
| 查询学生选课记录 | `GET /admin/students/{studentId}/selections` | 查询 | 供代选弹窗展示 |
| 教务代选 | `POST /admin/students/{studentId}/selections?courseId=&reason=` | 新增 | 写入 `select_type = 2` |
| 教务代退选 | `DELETE /admin/students/{studentId}/selections/{courseId}?reason=` | 退选 | 保留痕迹，不物理删除 |

**Service 方法签名建议**：

```java
/** 查询指定学生的全部选课记录（管理员视角） */
PageResult<CourseSelection> pageStudentSelections(Long studentId, Long semesterId);

/** 教务代选：绕过选课时间窗口，但保留其余全部校验 */
SelectionResultVO adminAddSelection(Long studentId, Long courseId, String reason);

/** 教务代退选：已录入成绩的记录不可退选 */
void adminRemoveSelection(Long studentId, Long courseId, String reason);
```

#### 2.6.3 校验规则（核心）

代选与普通选课的**关键差异**在于时间窗口，其余校验**一个都不能少**：

| 校验项 | 普通选课 | 教务代选 | 失败状态码 |
| --- | --- | --- | --- |
| 全局配置开关 `elective.selection.enabled` | ✅ 校验 | ❌ **跳过** | - |
| Redis 动态开关 `selection-switch` | ✅ 校验 | ❌ **跳过** | - |
| 学期 `status` 与选课时间窗口 | ✅ 校验 | ❌ **跳过** | - |
| 课程状态 `course.status = 1` | ✅ 校验 | ✅ **保留** | 3001 |
| 重复选课（`status = 1` 已存在） | ✅ 校验 | ✅ **保留** | 3003 |
| 学分上限 | ✅ 校验 | ✅ **保留** | 3008 |
| 上课时间冲突 | ✅ 校验 | ✅ **保留** | 3005 |
| 课程容量上限 | ✅ 校验 | ✅ **保留** | 3002 |
| 已录入成绩不可退选 | ✅ 校验 | ✅ **保留** | 3009 |

> **设计理由**：教务员代选本就是为「补救已过期的选课需求」而设，若仍受时间窗口限制则该功能失去意义。
> 权限风险通过 `select_type = 2` 留痕 + 操作日志来管控，而非通过时间卡口。

**代选专属规则**：

1. **必须填写代选原因**（`reason`），长度不少于 2 个字符，随操作日志一并落库，供后续审计追溯；
2. **容量已满时拒绝**，需管理员先扩容或由学生申请其他课程；
3. **写入选课记录时 `select_type` 置为 2**，用于区分正常选课与代选；
4. 被代选的学生若已有 `status = 0`（已退选）的历史记录，**复用该记录**：置 `status = 1`、更新 `select_time`、清空 `drop_time`、`select_type` 置 2。

#### 2.6.4 参考实现流程

```
adminAddSelection(studentId, courseId, reason)
  ↓
1. 校验 reason 非空且长度 ≥ 2        → 否则 参数错误
2. 校验学生存在且账号未被禁用         → 否则 学生不存在
3. 校验课程存在且 status = 1          → 否则 3001
4. 校验该学生本学期选课数 / 学分上限  → 否则 3008
5. 校验课程余量 > 0（读 Redis，兜底读库）→ 否则 3002
6. 时间冲突预检（四维判定）           → 冲突时抛 3005，由管理员二次确认后重试
  ↓
7. 写入选课记录（事务）
     ├─ 已有 status=1 记录 → 抛 3003
     ├─ 已有 status=0 记录 → 复用，置 status=1、selectType=2、清空 dropTime
     └─ 无记录             → 新增，status=1、selectType=2
  ↓
8. 更新 course.selected_count，并同步 Redis 余量
  ↓
9. 记录操作日志（含 reason）
```

#### 2.6.5 前端对应实现

前端已完整实现，位于 `frontend/src/views/admin/students.vue`：

- 学生列表操作列「**代选课程**」按钮 → 打开代选弹窗；
- 弹窗含两个页签：**已选课程**（可代退选）、**代选课程**（可搜索并代选）；
- 代选 / 代退选均通过 `ElMessageBox.prompt` **强制填写原因**；
- 已选学分 / 学分上限实时展示，超出时标红；
- 「选课方式」列以标签区分「正常选课」与「管理员代选」。

API 封装位于 `frontend/src/api/admin.js` 的「教务代选」分组。

#### 2.6.6 数据库变更

**无需新增表**。`course_selection.select_type` 字段已存在，取值 `1-正常选课 / 2-管理员代选`。

若需完整记录代选原因，可考虑以下两种方案（当前未实现）：

- **方案 A（轻量）**：原因仅写入 `sys_log` 操作日志，通过 `@OperationLog` 注解携带；
- **方案 B（完整）**：`course_selection` 新增 `select_reason VARCHAR(200)` 字段，随记录持久化。

> 当前前端已按「原因随请求提交」的方式实现，后端可先采用方案 A，若后续审计要求提高再升级为方案 B。

---

## 3. 成绩模块

### 3.1 需要实现的方法

在 `service/impl/CourseGradeServiceImpl.java` 中实现以下方法：

| # | 方法 | 优先级 | 复杂度 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | `inputGrades(GradeInputDTO)` | P0 | 中 | 待实现 |
| 2 | `publishGrades(Long courseId)` | P0 | 中 | 待实现 |
| 3 | `listCourseGradeSheet(Long courseId)` | P0 | 低 | **已实现** |
| 4 | `pageGrades(...)` | P1 | 低 | **已实现** |
| 5 | `getStudentGradeReport(Long semesterId)` | P1 | 中 | **已实现** |
| 6 | `revokeGrades(Long courseId)` | P2 | 低 | 待实现 |

**已实现方法说明**（第 3、4、5 项）：

- `pageGrades` —— 调用 `CourseGradeMapper#selectGradePage` 分页查询成绩。
- `listCourseGradeSheet` —— 查询课程并校验操作权限（`checkGradeAccess`：管理员不受限，
  教师仅能操作自己授课的课程，越权返回 403），随后按最多 1000 条加载该课程成绩，
  供成绩录入单展示。
- `getStudentGradeReport` —— 取当前学生**已发布**的成绩，逐条转换为成绩单条目，
  并统计总学分、已获学分、平均成绩与平均绩点：

  | 统计项 | 计算方式 |
  | --- | --- |
  | `totalCredit` | 所有课程学分求和，保留 1 位小数 |
  | `earnedCredit` | 仅及格课程学分求和，保留 1 位小数 |
  | `averageScore` | 有总评成绩课程的算术平均，保留 2 位小数 |
  | `averageGradePoint` | Σ(绩点 × 学分) / Σ(学分)，保留 2 位小数 |

  及格判定优先依据 `is_pass` 字段，缺失时按总评 ≥ 60 分推断。

> 这三个方法属纯查询与统计计算，不涉及并发控制与多表事务，已按标准增删查改实现。
>
> **注意**：`inputGrades`（成绩计算与落库）、`publishGrades`、`revokeGrades` 涉及
> 成绩计算规则、状态流转与 `course_selection` 联动更新，属核心业务，仍保留骨架待实现。

### 3.2 涉及的业务规则

#### 规则 1：成绩计算

**总评成绩**：

```
总评 = 平时成绩 × 0.3 + 期末成绩 × 0.7
```

**单边缺失时的折算**：

| 平时 | 期末 | 总评 |
| --- | --- | --- |
| 有 | 有 | `usual × 0.3 + exam × 0.7` |
| 有 | 无 | `usual` |
| 无 | 有 | `exam` |
| 无 | 无 | `null` |

结果保留 1 位小数（`RoundingMode.HALF_UP`）。

**绩点换算（4.0 制分段）**：

| 总评区间 | 绩点 |
| --- | --- |
| ≥ 90 | 4.0 |
| 85-89 | 3.7 |
| 82-84 | 3.3 |
| 78-81 | 3.0 |
| 75-77 | 2.7 |
| 72-74 | 2.3 |
| 68-71 | 2.0 |
| 64-67 | 1.5 |
| 60-63 | 1.0 |
| < 60 | 0.0 |

**及格判定**：总评 ≥ 60（`Constants.PASS_SCORE`）记为及格，`is_pass = 1`。

#### 规则 2：成绩取值范围

- 平时成绩、期末成绩均须在 **0-100** 之间，否则抛出 `4003`；
- 允许为 `null`（表示暂未录入该项）。

#### 规则 3：状态流转

```
草稿(0) ──发布──▶ 已发布(1) ──归档──▶ 已归档(2)
   ▲                  │
   └──────撤回────────┘
```

| 状态 | 常量 | 能否修改成绩 |
| --- | --- | --- |
| 草稿 | `GRADE_STATUS_DRAFT = 0` | ✅ 可修改 |
| 已发布 | `GRADE_STATUS_PUBLISHED = 1` | ❌ 需先撤回（抛 4002） |
| 已归档 | `GRADE_STATUS_ARCHIVED = 2` | ❌ 需先撤回（抛 4002） |

#### 规则 4：权限校验

```java
// 管理员可直接操作；教师仅能操作自己授课的课程
private void checkTeacherPermission(Course course) {
    LoginUser loginUser = SecurityUtils.getLoginUser();
    if (loginUser.isAdmin()) {
        return;
    }
    if (!course.getTeacherId().equals(loginUser.getTeacherId())) {
        throw new BusinessException(ResultCode.NOT_COURSE_TEACHER);  // 4004
    }
}
```

#### 规则 5：录入逻辑（`inputGrades`）

**流程**：

1. 校验 `courseId` 非空，查询课程并做权限校验；
2. 遍历 `dto.getItems()`：
   - 校验成绩范围；
   - **定位选课记录**：优先用 `selectionId`，其次用 `studentId`（需 `course_id` + `status = 1`）；
   - 查询是否已有成绩记录（按 `selectionId` 唯一）：
     - 已发布/已归档 → 抛出 `4002`；
     - 新增时初始化 `studentId`/`courseId`/`semesterId`/`status = 0`；
   - 计算总评与绩点，设置 `inputTime`、`inputBy`；
   - 保存（`saveOrUpdate`）；
   - **同步**：将总评写回 `course_selection.score`，便于学生端快速展示。

#### 规则 6：发布逻辑（`publishGrades`）

**前置条件**：

- 权限校验通过；
- 课程存在草稿状态的成绩记录，否则抛「暂无可发布的成绩记录」；
- **所有草稿记录的总评成绩均不为 `null`**，否则抛出：
  ```
  还有 N 名学生未录入成绩，无法发布
  ```

**执行**：

1. 批量将草稿记录置为已发布，设置 `publishTime`；
2. **数据联动**：将对应选课记录 `status` 从 `1`（已选课）改为 `2`（已修完）。

> ⚠️ 注意：`course_selection` 状态改为 2 后，「我的选课」与课表查询的过滤条件
> 可能需要同步调整（课表查询当前只查 `status = 1`）。

#### 规则 7：撤回逻辑（`revokeGrades`）

- 权限校验；
- 将已发布记录改回草稿，清空 `publishTime`；
- 建议同时将选课记录状态从 `2` 改回 `1`（保持数据一致）。

#### 规则 8：成绩单统计（`getStudentGradeReport`）

**查询**：`CourseGradeMapper.selectStudentGrades(studentId, semesterId, publishedOnly = true)`
（仅统计已发布成绩）。

**统计项**：

| 指标 | 计算方式 |
| --- | --- |
| `totalCredit` | 所有课程学分求和 |
| `earnedCredit` | 仅及格课程的学分求和 |
| `averageScore` | 所有有总评成绩的课程的算术平均，保留 2 位小数 |
| `averageGradePoint` | `Σ(绩点 × 学分) / Σ(学分)`，保留 2 位小数 |

### 3.3 接口与状态码对照

| 接口 | 路径 | 方法 | 可能返回的状态码 |
| --- | --- | --- | --- |
| 成绩录入单 | `GET /teacher/courses/{id}/grade-sheet` | `listCourseGradeSheet` | 3001/4004 |
| 批量录入 | `POST /teacher/grades/input` | `inputGrades` | 3001/4002/4003/4004/5001 |
| 发布成绩 | `POST /teacher/courses/{id}/grades/publish` | `publishGrades` | 3001/4004/5001 |
| 撤回成绩 | `POST /teacher/courses/{id}/grades/revoke` | `revokeGrades` | 3001/4004/5001 |
| 我的成绩单 | `GET /student/grades/report` | `getStudentGradeReport` | - |
| 已发布成绩 | `GET /student/grades` | `pageGrades` | - |

### 3.4 所需依赖注入

```java
private final CourseMapper courseMapper;
private final CourseSelectionMapper selectionMapper;
private final SemesterMapper semesterMapper;
```

---

## 4. 教学评价模块

> **状态**：**后端 + 前端均已实现并编译通过（2026-09-20）**。
> 本节保留完整设计文档作为实现说明与验收依据。
> 本模块不涉及并发与缓存，无 Redis Lua 需求。

### 4.1 业务背景与高校真实做法

学生评教（教学评价）是国内高校每学期末的**强制性教学环节**，
与选课、成绩并列为教务系统三大核心业务之一。

| 环节 | 真实做法 |
| --- | --- |
| 参评资格 | 仅**已修完**该课程的学生（`course_selection.status = 2`） |
| 参评时机 | 期末成绩录入后、成绩发布前后均可（各校不同，本项目取「已修完」即可评） |
| 匿名性 | **对学生、教师双向匿名**，仅管理员在必要审计时可见学号 |
| 一课一评 | 每位学生对每门课程**仅一份**评价，公开前可修改 |
| 结果用途 | 教师教学质量改进、课程评估；**不与任何考核排序强绑定**（避免刷分） |
| 违规处理 | 管理员可隐藏（下架）或删除明显违规/恶意的评价 |

> ⚠️ **「教务代评」不存在**。评价必须由学生本人完成，管理员无权代替学生提交，
> 这与选课可代为代选的情况不同，切勿照搬 2.6 节的代选思路。

### 4.2 数据模型（已新增）

**表 `course_review`**（编号 3.14，DDL 见 `db/schema.sql`）：

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| selection_id | BIGINT | NOT NULL, UNIQUE | 选课记录ID（**一选课一评价**，天然满足「一课一评」） |
| student_id | BIGINT | NOT NULL, INDEX | 学生ID |
| course_id | BIGINT | NOT NULL, INDEX | 课程ID |
| teacher_id | BIGINT | INDEX | 授课教师ID（冗余，便于按教师统计） |
| semester_id | BIGINT | NOT NULL, INDEX | 学期ID |
| score_content | TINYINT | NOT NULL | 教学内容（1-5） |
| score_teaching | TINYINT | NOT NULL | 教学方法（1-5） |
| score_attitude | TINYINT | NOT NULL | 教学态度（1-5） |
| score_gain | TINYINT | NOT NULL | 学习收获（1-5） |
| average_score | DECIMAL(3,1) | NOT NULL | 综合评分（四维平均，冗余便于统计排序） |
| content | VARCHAR(500) | | 文字评价 |
| anonymous | TINYINT | NOT NULL DEFAULT 1 | 是否匿名：0-否 1-是 |
| status | TINYINT | NOT NULL DEFAULT 1 | 0-草稿 1-已提交 2-已公开 3-已隐藏 |
| submit_time | DATETIME | | 提交时间 |
| create_time / update_time | DATETIME | NOT NULL | 审计字段 |
| deleted | TINYINT | NOT NULL DEFAULT 0 | 逻辑删除（管理员删除评价用） |

**索引**：

```sql
UNIQUE KEY `uk_selection_id` (`selection_id`),
KEY `idx_course_status` (`course_id`, `status`),
KEY `idx_teacher_semester` (`teacher_id`, `semester_id`),
KEY `idx_student_semester` (`student_id`, `semester_id`)
```

> **设计说明**：以 `selection_id` 作为唯一键，而不是 `(student_id, course_id)`，
> 是因为重修场景下同一学生同一课程可能存在多条选课记录，各自应独立评价。

**新增常量（`Constants`）**：

```java
/** 评价状态：草稿 */
public static final Integer REVIEW_STATUS_DRAFT = 0;
/** 评价状态：已提交（学生可见，教师不可见） */
public static final Integer REVIEW_STATUS_SUBMITTED = 1;
/** 评价状态：已公开（教师可见） */
public static final Integer REVIEW_STATUS_PUBLISHED = 2;
/** 评价状态：已隐藏（违规下架） */
public static final Integer REVIEW_STATUS_HIDDEN = 3;

/** 评价维度数量，用于计算综合评分 */
public static final int REVIEW_DIMENSION_COUNT = 4;
```

**新增状态码（`ResultCode`，延续 4xxx 成绩段之后另起 6xxx 段）**：

| 状态码 | 常量 | 场景 |
| --- | --- | --- |
| 6001 | `REVIEW_NOT_FOUND` | 评价记录不存在 |
| 6002 | `REVIEW_ALREADY_EXISTS` | 已评价过该课程 |
| 6003 | `REVIEW_NOT_ALLOWED` | 课程未修完，暂无评价资格 |
| 6004 | `REVIEW_CONTENT_ILLEGAL` | 文字评价长度或分数越界（1-5 分、5-500 字） |
| 6005 | `REVIEW_HIDDEN` | 评价已被管理员隐藏，无法修改 |

> 上述常量与状态码**均已落地**（`Constants`、`ResultCode`）。
> 越权修改/查看他人评价复用既有 `FORBIDDEN(403)` 与 `ROLE_NOT_ALLOWED(2007)`。

### 4.3 接口设计（已实现）

前后端均按下列契约实现，共 15 个接口。

**学生端**：

| 方法 | 路径 | 方法名 | 说明 |
| --- | --- | --- | --- |
| GET | `/student/reviews/pending` | `listPendingReviews(semesterId)` | 待评价课程列表（已修完且未评价） |
| POST | `/student/reviews` | `submitReview(CourseReviewDTO)` | 提交/修改评价（有 id 为修改） |
| GET | `/student/reviews` | `listMyReviews(semesterId)` | 我的评价列表 |
| GET | `/student/reviews/{courseId}` | `getMyReview(courseId)` | 我对某课程的评价详情 |
| DELETE | `/student/reviews/{id}` | `deleteMyReview(id)` | 撤回我的评价 |
| GET | `/student/courses/{courseId}/review-summary` | `getCourseReviewSummary(courseId)` | 课程评价汇总 |
| GET | `/student/courses/{courseId}/reviews` | `listCourseReviews(courseId, page)` | 课程公开评价列表（匿名） |

**教师端（只读）**：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/teacher/reviews/summary` | 本人授课课程的评价汇总列表（semesterId） |
| GET | `/teacher/courses/{courseId}/reviews` | 指定课程的评价汇总 + 公开评价明细 |

**管理员端**：

| 方法 | 路径 | 方法名 | 说明 |
| --- | --- | --- | --- |
| GET | `/admin/reviews` | `pageReviews(query)` | 分页查询（keyword / semesterId / status / scoreLevel） |
| PUT | `/admin/reviews/{id}/status/{status}` | `updateReviewStatus(id, status, reason)` | 公开(2) / 隐藏(3) |
| DELETE | `/admin/reviews/{id}` | `deleteReview(id, reason)` | 逻辑删除（`reason` 记入操作日志） |
| GET | `/admin/reviews/statistics` | `getReviewStatistics(semesterId)` | 评价总数、公开数、隐藏数、平均分、参评率、覆盖课程数 |

**`CourseReviewDTO`（前端已按此提交）**：

```json
{
  "id": null,
  "courseId": 1,
  "scoreContent": 5,
  "scoreTeaching": 4.5,
  "scoreAttitude": 5,
  "scoreGain": 4,
  "content": "讲解细致，案例贴近实战。",
  "anonymous": 1
}
```

**汇总 VO（前端卡片与明细均按此渲染）**：

```json
{
  "courseId": "1",
  "courseCode": "GE1001",
  "courseName": "Python程序设计基础",
  "teacherName": "张明远",
  "semesterName": "2026-2027学年第一学期",
  "averageScore": 4.6,
  "scoreContent": 4.7,
  "scoreTeaching": 4.5,
  "scoreAttitude": 4.8,
  "scoreGain": 4.4,
  "reviewCount": 32,
  "studentCount": 45
}
```

> 前端已做**容错**：`/teacher/courses/{id}/reviews` 返回「明细数组」或
> `{ summary, reviews }` 两种结构均可正常渲染。

### 4.4 涉及的业务规则

#### 规则 1：参评资格校验（核心）

提交评价前必须校验，任一条不满足即拒绝：

| 校验项 | 判定 | 失败状态码 |
| --- | --- | --- |
| 学生身份 | `SecurityUtils.requireStudentId()` 取当前学生 | 401 |
| 选课记录存在 | `course_selection` 中 `student_id + course_id + status = 2`（已修完） | 6003 |
| 学期匹配 | 选课记录的 `semester_id` 用于写入评价 | - |
| 重复评价 | 该 `selection_id` 尚无有效评价（新增时） | 6002 |
| 评价归属 | 修改时评价的 `student_id` 必须为当前学生 | 403 |

```java
// 参考骨架（请勿照抄，实现时按项目规范补全）
CourseSelection selection = selectionMapper.selectOne(
        Wrappers.<CourseSelection>lambdaQuery()
                .eq(CourseSelection::getStudentId, studentId)
                .eq(CourseSelection::getCourseId, courseId)
                .eq(CourseSelection::getStatus, Constants.SELECTION_FINISHED));
if (selection == null) {
    throw new BusinessException(ResultCode.REVIEW_NOT_ALLOWED, "该课程尚未修完，暂无评价资格");
}
```

> **取舍说明**：本项目取「已修完（`status = 2`）」作为参评条件。
> 若希望放宽为「已选课即可评」，需注意成绩未出时学生可能因成绩不满意而恶意评价，
> 故多数高校要求成绩已录入（无论是否发布）。若采用更严的「成绩已发布」条件，
> 需 JOIN `course_grade` 校验 `status = 1`。

#### 规则 2：评分与内容校验

- 四个维度均为 **1-5 分**（支持半星，即 `.5` 步长），任一为空或越界 → `6004`；
- `average_score` = 四维算术平均，保留 **1 位小数**（`RoundingMode.HALF_UP`）；
- `content` 长度 **5-500** 字，超限或过短 → `6004`；
- `anonymous` 缺省为 `1`（匿名）。

```java
BigDecimal avg = BigDecimal.valueOf(
        (scoreContent + scoreTeaching + scoreAttitude + scoreGain) / 4.0)
        .setScale(1, RoundingMode.HALF_UP);
```

#### 规则 3：状态流转

```
草稿(0) ──提交──▶ 已提交(1) ──管理员公开──▶ 已公开(2)
                     ▲                        │
                     └──── 管理员隐藏 ────────┤
                                              ▼
                                          已隐藏(3)
```

| 状态 | 学生可修改 | 教师可见 | 说明 |
| --- | --- | --- | --- |
| 0 草稿 | ✅ | ❌ | 预留状态，当前前端不产生 |
| 1 已提交 | ✅ | ❌ | **默认状态**，学生可随时修改 |
| 2 已公开 | ✅ | ✅ | 管理员审核公开后教师才可见 |
| 3 已隐藏 | ❌（抛 6005） | ❌ | 违规下架，学生端按钮置灰 |

> **简化方案（可选）**：若追求「提交即公开」，可省略审核环节，
> 提交时直接置 `status = 2`，管理员仅保留**事后隐藏/删除**能力。
> 前端对两种方案均兼容（状态标签与按钮禁用逻辑已按 4 个状态实现）。

#### 规则 4：匿名与脱敏

- **学生端**：只看得到他人的公开评价，展示 `anonymous == 1 ? '匿名同学' : 脱敏姓名`；
- **教师端**：同样只展示脱敏信息，**不得下发** `studentId` / `stuNo`；
- **管理员端**：可查看完整学号（审计需要），前端已在「评价人」列做区分展示；
- 脱敏规则统一为「张*远」（`frontend/src/utils/dict.js` 的 `maskName`，后端如有导出需求请保持一致）。

> ⚠️ **后端务必在 VO 层面过滤敏感字段**，不要直接把 `CourseReview` 实体返回给教师端。
> 这是本模块最容易被后来人简化的地方——一旦实体直出，匿名机制即告失效。

#### 规则 5：统计口径

| 指标 | 计算方式 |
| --- | --- |
| `averageScore`（课程） | 该课程所有**非隐藏**评价的 `average_score` 平均值，保留 1 位 |
| 各维度得分 | 对应维度字段的平均值，保留 1 位 |
| `reviewCount` | 该课程非隐藏评价数 |
| `studentCount` | 该课程 `status = 2` 的选课人数（参评率分母） |
| `participateRate` | `reviewCount / studentCount`，百分比取整 |
| 全校 `averageScore` | 所有非隐藏评价 `average_score` 的平均值 |

> **`studentCount` 必须取「已修完人数」而非「已选人数」**，
> 否则参评率会被未修完的学生稀释，失去参考意义。

#### 规则 6：管理员操作留痕

隐藏与删除均**必须填写原因**（≥ 2 字），且随 `@OperationLog` 落库：

```java
@OperationLog(module = "教学评价", operation = "隐藏评价")
@PutMapping("/{id}/status/{status}")
public Result<Void> updateStatus(@PathVariable Long id,
                                 @PathVariable Integer status,
                                 @RequestParam String reason) {
    reviewService.updateStatus(id, status, reason);
    return Result.success();
}
```

删除采用**逻辑删除**（`deleted = 1`），保留审计痕迹；
删除后该 `selection_id` 的 UNIQUE 约束会阻挡重新提交，
**实现时需注意**：建议改为「物理删除」或把唯一键调整为
`(selection_id, deleted)` 组合，或删除时把 `selection_id` 置负值。
（推荐**物理删除**，评价数据无强审计要求，实现最简单。）

### 4.5 实现文件清单（**已完成**）

| 层 | 文件 | 说明 |
| --- | --- | --- |
| 建表 | `db/schema.sql` | 新增 `course_review` 表（含唯一键与 3 个索引），DROP 清单同步补入 |
| 常量 | `common/Constants.java` | `REVIEW_STATUS_*`、维度分值区间、文字长度、匿名标记、原因最小长度 |
| 状态码 | `common/ResultCode.java` | `6001-6005` 评价相关业务错误 |
| 实体 | `entity/CourseReview.java` | 16 个数据库字段 + 6 个展示用非数据库字段 |
| DTO | `dto/CourseReviewDTO.java`、`dto/CourseReviewQueryDTO.java` | 提交参数（JSR-303 校验）、查询条件（含 `scoreLevel`） |
| VO | `vo/CourseReviewVO.java`、`vo/CourseReviewSummaryVO.java`、`vo/CourseReviewDetailVO.java`、`vo/PendingReviewVO.java`、`vo/ReviewStatisticsVO.java` | 匿名脱敏在 VO 层完成 |
| Mapper | `mapper/CourseReviewMapper.java` + `resources/mapper/CourseReviewMapper.xml` | 9 个自定义查询方法 |
| Service | `service/CourseReviewService.java` + `impl/CourseReviewServiceImpl.java` | 14 个方法，核心业务全部落地 |
| Controller | `controller/StudentReviewController.java`、`TeacherReviewController.java`、`AdminReviewController.java` | 路径 `/student/**`、`/teacher/**`、`/admin/**`，由 `SecurityConfig` 既有规则覆盖，**无需改动安全配置** |

**依赖注入**：

```java
private final CourseMapper courseMapper;                    // 课程详情、授课教师
private final CourseSelectionMapper selectionMapper;        // 参评资格、参评率分母
```

> 实际实现仅需上述两个 Mapper（实体自身 Mapper 由 `ServiceImpl` 提供），
> 比设计阶段预估的依赖更少。无需 Redis。

**关键实现细节**：

1. **综合评分后端计算**：`CourseReviewDTO` 不接收 `averageScore`，
   由 `calculateAverage()` 依据四维取值计算，防止前端篡改为不合理高分。
2. **匿名脱敏集中在 `toVO()`**：三参版本用 `keepOwn` / `keepIdentity`
   区分「本人查看」「管理员审计」「他人查看」三种场景，避免各处重复判断。
3. **重新提交回退状态**：学生修改评价后状态回到「已提交」（`status = 1`），
   需管理员重新公开，避免学生先获得好评再改差评绕过审核。
4. **物理删除**：`removeById()` 走的是 MyBatis-Plus 逻辑删除，
   但 `course_review` 表**未定义 `deleted` 字段**，因此实际为物理删除，
   保证 `selection_id` 唯一键释放、学生可重新提交。

### 4.6 前端实现（**已完成**）

| 文件 | 角色 | 说明 |
| --- | --- | --- |
| `frontend/src/views/student/reviews.vue` | 学生 | 待评价卡片 + 我的评价表格 + 四维评分表单 |
| `frontend/src/views/teacher/reviews.vue` | 教师 | 评价汇总卡片（四维得分）+ 匿名评价明细弹窗 |
| `frontend/src/views/admin/reviews.vue` | 管理员 | 统计概览 + 评价列表 + 隐藏/删除（强制填原因） |
| `frontend/src/api/student.js` | - | 评价相关 7 个接口封装 |
| `frontend/src/api/teacher.js` | - | 评价只读 2 个接口封装 |
| `frontend/src/api/admin.js` | - | 评价管理 4 个接口封装 |
| `frontend/src/utils/dict.js` | - | `REVIEW_STATUS_MAP`、`REVIEW_DIMENSIONS`、`averageScore`、`scoreTagType`、`maskName` |
| `frontend/src/router/index.js` | - | 三个角色各注册 `reviews` 路由（`icon: ChatDotSquare`） |

**页面能力明细**：

- 学生端
  - 「待评价课程」卡片列表，点「去评价」打开表单；
  - 四维 `el-rate`（支持半星）+ 自动计算的综合评分标签；
  - 文字评价 5-500 字校验；匿名开关；
  - 「我的评价」表格可按状态查看，支持**修改**与**撤回**；
  - 状态为「已隐藏」时修改按钮置灰。
- 教师端
  - 课程卡片展示平均分与四个维度得分（`el-rate` 只读）；
  - 底部展示「参评 X / Y 人」，点「查看评价」弹出匿名明细。
- 管理员端
  - 顶部 6 张统计卡片（评价总数 / 已公开 / 已隐藏 / 平均分 / 参评率 / 覆盖课程）；
  - 支持课程关键词、学期、状态、评分档位（高/良好/一般/偏低）筛选；
  - 「隐藏 / 公开」与「删除」均通过 `ElMessageBox.prompt` **强制填写原因**。
- 学生「我的选课」页
  - `status = 2`（已修完）的记录显示「去评价」按钮或「已评价」标签
    （依赖后端在选课记录中回传 `reviewed` 布尔字段）。

### 4.7 小程序端（**已实现**）

> 评价是小程序端**最合适的场景**：学生期末在手机上即可完成，无需打开电脑。

**工程位置**：`miniprogram/`（与 `frontend/`、`backend/` 同级），
使用**原生微信小程序**（WXML / WXSS / JS），无构建依赖，
用微信开发者工具直接导入即可运行。详见 `miniprogram/README.md`。

**技术选型说明**：项目未采用 uni-app，原因有三：

1. 不需要 H5 / App 端，网页端已有完整 Vue3 工程，uni-app 的多端价值无从体现；
2. 避免引入第二套构建链（Vite + uni 插件），与现有 `frontend/` 的依赖体系共存成本高；
3. Element Plus 无法复用，`uni-ui` 是另一套组件 API，学习成本反而不低。

**已实现页面**：

| 页面 | 路径 | 功能 |
| --- | --- | --- |
| 登录 | `pages/login/index` | 学号密码登录，复用 `/auth/login`，仅放行 STUDENT 角色 |
| 待评价列表 | `pages/review/list/index` | tabBar 首页，卡片列表 + 已评计数 + 学期筛选 |
| 评价表单 | `pages/review/form/index` | 四维星级（半星）、快捷标签、匿名开关、二次确认提交 |
| 我的评价 | `pages/review/mine/index` | 已提交评价，支持修改与撤回，已隐藏评价置灰 |
| 评价详情 | `pages/review/detail/index` | 汇总 + 四维得分条 + 匿名明细（触底分页加载） |
| 我的课表 | `pages/timetable/index` | 按星期分组列表，含节次时间、周次、地点 |
| 我的成绩 | `pages/grade/index` | 总学分/已获学分/平均分/平均绩点 + 成绩明细 |
| 个人中心 | `pages/mine/index` | 用户信息、功能入口、退出登录 |

**交互差异（相对 PC 端）**：

- 星级用**大号可点区域**（56rpx ≈ 28px），点击左半区得 .5 分、右半区得整分，
  自绘实现而未引入第三方评分组件；
- 文字评价提供**快捷标签**，点选自动拼接为「讲解清晰，条理分明，」形式，
  再次点击可取消并同步移除文本；
- 提交前 `wx.showModal` 二次确认，提交后 toast 并返回上一页；
- 匿名开关默认开启，下方一行小字说明匿名范围；
- 课表由网格改为**按星期分组的列表**（窄屏下网格课表可读性差）。

**关键实现约定**（与 Web 端保持一致，切勿单侧修改）：

- 后端返回 `code === 200` 视为成功，业务数据在 `data` 字段；
- 401 与业务码 `2003` 均视为登录失效，清理缓存后 `reLaunch` 到登录页，
  并用 `isHandlingUnauthorized` 标记位防止并发请求重复跳转；
- 综合评分仅在前端做**预览展示**，最终值仍由后端计算；
- 评价状态 3（已隐藏）时，修改与撤回入口均被拦截。

**接口对接**：全部复用既有接口，**后端无需为小程序新增任何接口**。
`baseUrl` 配置在 `miniprogram/config/index.js`，部署前需改为实际地址。

#### 4.7.1 需要后端支持的增强功能（**待实现**）

以下能力小程序当前**未做**，如后续需要，必须由后端先行支持：

| 功能 | 后端缺口 | 说明 |
| --- | --- | --- |
| **微信一键登录** | 需新增 `sys_user.openid` 字段（或独立的 `user_wx_bind` 表）+ `POST /auth/wx-login` 接口 | 用户授权后前端拿到 `code`，后端调微信 `jscode2session` 换取 `openid`，据此匹配或绑定账号后签发 JWT。**关键安全点**：不能仅凭 `openid` 直接签发令牌，必须先确认已绑定系统账号，否则任一微信用户可登入 |
| **订阅消息推送** | 需新增消息模板配置 + `POST /message/subscribe` 记录订阅关系 + 发送服务 | 场景如「评价开放提醒」「成绩发布提醒」。需申请微信模板 ID，且用户每次授权仅能推送一次，需设计订阅关系续期 |
| **评价数据可视化** | 可选，需新增 `GET /student/reviews/trend` 返回按学期聚合的评分趋势 | 用于小程序端绘制各维度雷达图与学期对比折线 |
| **评价开放时间校验** | 需扩展 `submitReview`，增加「学期评价窗口」配置 | 当前仅校验「已修完」，部分高校要求评价仅在指定时间段内开放。若需要，可在 `semester` 表增加 `review_start_time` / `review_end_time` 两字段 |

> ⚠️ 上述四项**均未实现**，且都不是小程序单侧能解决的——
> `openid` 绑定与消息推送涉及账号体系安全，评价窗口涉及本学期业务规则，
> 请在实现时参照本文档的规则设计习惯，先在 Service 层完成校验再暴露接口。

**其他待办**：

- 小程序**分享卡片**（`onShareAppMessage`）尚未实现；
- 未做页面级骨架屏，弱网下首次加载仅有「加载中」文字提示；
- 正式发布需 HTTPS 域名并在小程序后台配置 `request` 合法域名。

### 4.8 数据库变更说明

1. `db/schema.sql` 已新增 `course_review` 表，并在文件头 `DROP TABLE` 清单
   补入 `DROP TABLE IF EXISTS course_review;`；
2. 开发环境由 `DatabaseInitializer` 自动建表（含索引，因其直接执行完整
   `CREATE TABLE` 语句），应用重启即生效，**无需手工执行脚本**；
3. `docs/数据库设计.md` 已在第 2 节表清单加入第 14 行，并在第 3 节补充
   `3.14 course_review` 结构说明。

> ⚠️ **建表前提**：`DatabaseInitializer` 依赖数据库连接。若启动日志出现
> `Communications link failure`，说明 MySQL 未就绪，此时需手工执行
> `scripts/init-db.sql` 或 `db/schema.sql` 完成建表。

---

## 5. 已就绪的基础设施

### 5.1 Lua 脚本

| 脚本 | 路径 | 已在何处注册 |
| --- | --- | --- |
| `select_course.lua` | `resources/lua/select_course.lua` | `RedisConfig#selectCourseScript` |
| `drop_course.lua` | `resources/lua/drop_course.lua` | `RedisConfig#dropCourseScript` |

**选课脚本返回码**：`0` 成功 / `1` 已选过 / `2` 余量不足 / `3` 缓存未就绪

**退课脚本返回码**：`0` 成功 / `1` 未选过该课程

### 5.2 Redis Key 约定

| 常量 | 实际 Key | 类型 | 用途 |
| --- | --- | --- | --- |
| `RedisKeys.COURSE_CAPACITY` | `elective:course:capacity:{courseId}` | String | 课程剩余容量 |
| `RedisKeys.COURSE_SELECTED` | `elective:course:selected:{courseId}` | Set | 已选学生ID集合 |
| `RedisKeys.SELECTION_SWITCH` | `elective:selection:switch` | String | 选课总开关（on/off） |
| `RedisKeys.CURRENT_SEMESTER` | `elective:semester:current` | String | 当前学期缓存 |

### 5.3 Mapper 可用方法

**`CourseSelectionMapper`**

| 方法 | 说明 |
| --- | --- |
| `selectStudentSelections(page, studentId, semesterId, status)` | 学生选课分页（联表课程/教师） |
| `selectCourseStudents(page, courseId, keyword)` | 课程学生名单分页（联表学生） |
| `selectSelectedCourseIds(studentId, semesterId)` | 已选课程ID列表 |
| `sumSelectedCredit(studentId, semesterId)` | 已选学分合计 |
| `countBySemester(semesterId)` | 学期选课总数 |

**`CourseGradeMapper`**

| 方法 | 说明 |
| --- | --- |
| `selectGradePage(page, studentId, courseId, semesterId, status)` | 成绩分页（联表学生/课程） |
| `selectStudentGrades(studentId, semesterId, publishedOnly)` | 学生成绩单明细 |
| `selectScoreDistribution(courseId, semesterId)` | 分数段分布统计 |

**`CourseScheduleMapper`**

| 方法 | 说明 |
| --- | --- |
| `selectByCourseId(courseId)` | 课程排课明细 |
| `selectByCourseIds(courseIds)` | 批量排课（课表） |
| `selectByStudentSelection(studentId, semesterId)` | 学生已选课程的排课（**冲突检测用**） |
| `countTeacherConflict(...)` | 教师时间冲突计数 |
| `countClassroomConflict(...)` | 教室占用冲突计数 |

**`CourseMapper`**（选课相关）

| 方法 | 说明 |
| --- | --- |
| `increaseSelectedCount(courseId)` | 数据库侧 +1（带余量判断） |
| `decreaseSelectedCount(courseId)` | 数据库侧 -1 |
| `syncSelectedCount(courseId)` | 按真实选课数重置计数 |

### 5.4 相关常量

```java
// Constants
SELECTION_DROPPED  = 0;   // 已退选
SELECTION_SELECTED = 1;   // 已选课
SELECTION_FINISHED = 2;   // 已修完

GRADE_STATUS_DRAFT     = 0;
GRADE_STATUS_PUBLISHED = 1;
GRADE_STATUS_ARCHIVED  = 2;

PASS_SCORE = 60.0;

WEEK_TYPE_ALL  = "ALL";   // 每周
WEEK_TYPE_ODD  = "ODD";   // 单周
WEEK_TYPE_EVEN = "EVEN";  // 双周
```

### 5.5 参考的算法实现

排课冲突判定逻辑的**完整参考实现**（含 7 个测试用例）位于：

```
backend/src/test/java/com/college/elective/ScheduleConflictTests.java
```

可直接将其 `isConflict` 方法逻辑迁移到 Service 中。

---

## 6. 实现顺序建议

```
阶段一：选课基础查询（快速见效）
  ├─ pageAvailableCourses      查询课程 + 补充 Redis 余量 + selected 标记
  ├─ pageMySelections          直接调用 Mapper                        【已完成】
  └─ pageCourseStudents        直接调用 Mapper                        【已完成】
        ↓
阶段二：时间冲突检测
  └─ checkConflict             实现四维判定（已提取 ScheduleConflictUtils）【已完成】
        ↓
阶段三：选课与退课（核心）
  ├─ 缓存回源逻辑               ensureCacheReady / refreshCourseCache
  ├─ selectCourse              Lua 预占 + 落库 + 回滚
  └─ dropCourse                状态更新 + Lua 归还
        ↓
阶段四：缓存运维
  ├─ preloadCourseCache        批量重建缓存
  └─ syncSelectionCount        一致性校正（完成后启用定时任务）
        ↓
阶段五：成绩模块
  ├─ listCourseGradeSheet      录入单查询
  ├─ pageGrades                分页查询
  ├─ inputGrades               录入 + 计算 + 同步
  ├─ publishGrades / revokeGrades
  └─ getStudentGradeReport     成绩单统计
        ↓
阶段六：教学评价模块（独立，无并发要求）
  ├─ 建表 course_review + 常量 + 状态码                        【已完成】
  ├─ listPendingReviews        待评价课程（已修完名单 - 已评价）   【已完成】
  ├─ submitReview              四维校验 + 综合评分 + 落库        【已完成】
  ├─ listMyReviews / getMyReview / deleteMyReview             【已完成】
  ├─ getCourseReviewSummary    课程汇总（平均分 + 参评率）        【已完成】
  ├─ listCourseReviews         课程公开评价（匿名，注意 VO 脱敏）   【已完成】
  ├─ 教师端只读查询              summary / 课程明细               【已完成】
  └─ 管理员端                   pageReviews / status / delete / statistics 【已完成】
```

> **教学评价模块已全部实现**，不依赖 Redis、无并发复杂度，
> 可作为其他待实现模块（选课、成绩）的参考范式——
> 特别是「匿名脱敏放在 VO 层」与「综合分后端计算」这两处防御性设计。

---

## 7. 验收清单

### 7.1 选课模块

- [ ] 可选课程列表能正确显示 Redis 实时余量
- [ ] 已选课程在学生视角正确标记 `selected = true`
- [ ] 并发选课（如 100 并发抢 50 容量）后，成功数恰为 50，无超选
- [ ] 重复选课被拒绝，返回 3003
- [ ] 时间冲突（同天、节次重叠、周次重叠、周类型兼容）被拒绝，返回 3005
- [ ] 单双周不冲突的场景能正常选课
- [x] 冲突预检返回冲突课程名称、编号与时间信息（`checkConflict` 已实现）
- [x] 同一天不同节次（如周一 1-2 节与周一 5-6 节）不误判为冲突
- [x] 未设置当前学期时冲突预检返回 5005，而非静默返回空结果
- [x] 已选同一课程时不误报自身冲突
- [ ] 超出学分上限被拒绝，返回 3008
- [ ] 选课关闭时（Redis 开关 off）被拒绝，返回 3007
- [ ] 学期 `status != 1` 时选课被拒绝，返回 3006
- [ ] 不在选课时间窗口内（早于开放时间或晚于截止时间）被拒绝，返回 3006
- [ ] `status = 1` 但时间窗口外（或反之）时，按既定的交集策略正确拒绝
- [ ] 绕过前端直接调用选课接口时，后端校验依然生效
- [ ] 落库失败时 Redis 余量正确归还（可模拟异常验证）
- [ ] 退课后余量正确 +1，选课记录状态变为 0
- [ ] 已录入成绩的课程无法退课
- [ ] 缓存预热后 Redis Key 数量与课程数一致
- [ ] 手工制造数据偏差后，同步接口能校正

**教务代选**：

- [ ] 选课窗口关闭后，管理员代选依然成功（验证跳过时间窗口）
- [ ] 代选生成的记录 `select_type = 2`
- [ ] 未填写代选原因时请求被拒绝
- [ ] 代选时超出学分上限被拒绝，返回 3008
- [ ] 代选时上课时间冲突被拒绝，返回 3005
- [ ] 代选时课程已满被拒绝，返回 3002
- [ ] 代选已选过的课程被拒绝，返回 3003
- [ ] 代选已退选（`status = 0`）的课程时复用原记录，`select_type` 更新为 2
- [ ] 已录入成绩的课程无法代退选，返回 3009
- [ ] 代退选后记录 `status = 0` 且保留 `drop_time`，余量正确归还
- [ ] 教师端接口无法调用代选能力（越权返回 403）
- [ ] 代选原因已记入操作日志，可按操作人追溯

### 7.2 成绩模块

- [ ] 总评成绩计算正确（平时 30% + 期末 70%）
- [ ] 单边成绩缺失时按规则折算
- [ ] 绩点换算与分段表一致
- [ ] 成绩超出 0-100 被拒绝，返回 4003
- [ ] 已发布成绩无法直接修改，返回 4002
- [ ] 存在未录入成绩的学生时无法发布
- [ ] 发布后学生能查询到成绩，选课记录状态变为 2（已修完）
- [ ] 撤回后学生无法查询，可继续修改
- [ ] 教师只能操作自己课程的成绩，越权返回 4004
- [ ] 成绩单统计（总学分、已获学分、平均分、平均绩点）计算正确
- [ ] 成绩分布图表数据正常（需同时恢复 `CourseServiceImpl` 中的 `scoreDistribution`）

### 7.3 前端联调

- [ ] 选课成功/失败的提示信息对用户友好
- [ ] 时间冲突时能展示具体冲突课程与时间
- [x] 课程卡片余量进度条与实际数据一致
- [x] 学生「我的选课」分页查询可正常返回数据（`pageMySelections` 已实现）
- [x] 教师可查看自己课程的选课名单，访问他人课程返回 403（`pageCourseStudents` 已实现）
- [x] 管理员可查看任意课程的学生名单（`checkCourseAccess` 已实现）
- [x] 成绩分页查询可正常返回数据（`pageGrades` 已实现）
- [x] 成绩录入单可加载指定课程的成绩列表，越权访问返回 403（`listCourseGradeSheet` 已实现）
- [x] 学生成绩单仅统计已发布成绩，总学分/已获学分/平均成绩/平均绩点计算正确（`getStudentGradeReport` 已实现）
- [x] 成绩单中及格判定正确（优先取 `is_pass`，缺失时按总评 ≥ 60 推断）
- [ ] 未到选课开放时间时，学生端选课按钮为禁用状态且文案正确（未开始/已结束/未开放）
- [ ] 倒计时到 0 后按钮自动变为可点击，无需刷新页面
- [ ] 倒计时改用服务器时间基准后，修改本机时间不影响显示（见 2.5 节，待实现）

### 7.4 教学评价模块（后端已实现，待联调验证）

**参评资格**：

- [ ] 未修完（`status = 1`）的课程不出现在待评价列表
- [ ] 直接调用提交接口为未修完课程评价，返回 6003
- [ ] 同一学生对同一门课程重复提交，返回 6002
- [ ] 直接调用接口修改他人评价，返回 403
- [ ] 学生只能看到自己的评价记录，无法查看他人明细

**表单与校验**：

- [ ] 四维评分任一为空时无法提交
- [ ] 四维评分可打半星（如 4.5），落库精度正确
- [ ] 综合评分 = 四维平均且保留 1 位小数
- [ ] 文字评价少于 5 字或超过 500 字时被拒绝，返回 6004
- [ ] 匿名开关默认开启，关闭时教师端可见脱敏姓名

**状态与匿名**：

- [ ] 「已提交」状态的评价教师端不可见
- [ ] 管理员公开后教师端立即可见
- [ ] 管理员隐藏后学生端修改按钮置灰，强行提交返回 6005
- [ ] 教师端返回的 JSON **不含** `studentId` / `stuNo` 字段（抓包验证）
- [ ] 匿名评价在教师端与管理员端展示为「匿名同学」

**统计**：

- [ ] 课程平均分仅统计非隐藏评价
- [ ] 各维度平均分与明细数据一致
- [ ] 参评率分母为该课程「已修完」人数，而非「已选」人数
- [ ] 管理员统计卡片的 6 项指标与列表数据一致

**管理员操作**：

- [ ] 隐藏评价未填写原因时被拒绝
- [ ] 删除评价未填写原因时被拒绝
- [ ] 隐藏 / 删除原因已记入操作日志，可按操作人追溯
- [ ] 删除（撤回）后学生可重新提交同一门课程的评价
- [ ] 评分档位筛选（高 / 良好 / 一般 / 偏低）结果正确

**前端**：

- [ ] 「我的选课」页已修完记录显示「去评价」按钮或「已评价」标签
- [ ] 待评价数量与「我的评价」数量随提交、撤回实时刷新
- [ ] 撤回后评价回到待评价列表
- [ ] 三个角色的「教学评价」菜单均只对对应角色可见

---

## 8. 实现完成后的收尾工作

1. **调整 Controller 注入**：将 4 个 Controller 中的 `ObjectProvider<XxxService>` 改为直接注入 `XxxService`，
   并移除 `PendingFeature` 判空分支
2. **启用定时任务**：将 `SelectionSyncTask.TASK_ENABLED` 改为 `true`
3. **恢复成绩分布统计**：取消 `CourseServiceImpl#getStatistics` 中的注释并注入 `CourseGradeMapper`
4. **补充单元测试**：为选课并发、成绩计算与评价汇总统计补充测试用例
5. **更新本文档**：将对应条目标记为已完成，或直接归档本文档
6. **更新 README**：移除「待实现」相关说明
7. **同步评价模块文档**：为 `course_review` 表补充 `数据库设计.md` 的 3.14 节
   与 `接口文档.md` 的第 13 节

---

## 附：相关文档

| 文档 | 说明 |
| --- | --- |
| [架构设计.md](./架构设计.md) | 架构设计（选课并发方案详见 5.1 节） |
| [数据库设计.md](./数据库设计.md) | 数据库设计（选课/成绩表详见 3.10、3.11 节） |
| [接口文档.md](./接口文档.md) | 接口文档（选课详见第 02 节，成绩详见第 03、04 节） |
| [开发指南.md](./开发指南.md) | 开发指南（编码规范与调试技巧） |
| [更新日志.md](./更新日志.md) | 更新日志 |
