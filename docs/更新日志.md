# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 与 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范。

> ⚠️ **当前版本存在待实现功能**：选课模块与成绩模块的 Service 实现尚未编写，
> 详见 [待实现功能文档](./待实现功能.md)。

## [1.0.0] - 2026-09-11

### 新增

**基础框架**

- 集成 Spring Boot 3.3.5 + Java 21 + MyBatis-Plus 3.5.7 + MySQL 8 + Redis 7
- 搭建 Vue 3.5 + Vite 5 + Element Plus 2.8 + Pinia 2 前端工程
- 多环境配置（dev / prod）与 Logback 日志滚动策略
- 统一响应结构 `Result` / `PageResult` 与全局异常处理器
- Knife4j OpenAPI 3 接口文档

**认证与安全**

- JWT 令牌签发、验签与 Redis 白名单机制
- Spring Security 无状态认证，URL 级与方法级权限控制
- 登录失败计数与临时锁定（连续 5 次锁定 15 分钟）
- 令牌滑动续期（剩余有效期不足阈值自动续期）
- 密码 bcrypt 加密，修改密码后强制重新登录
- 401 / 403 统一 JSON 响应处理器

**数据模型**

- 14 张业务表设计与 DDL 脚本（`schema.sql`）
- 完整初始化数据（`data.sql`）：5 院系、9 专业、11 用户、10 课程、6 教室、3 学期、3 公告
- 逻辑删除、乐观锁（`course.version`）、自动填充创建/更新时间

**核心业务**

- **选课**：Redis Lua 脚本原子预占（查重 + 余量校验 + 扣减 + 记录），有效防止超选与重复选课
- **退课**：Lua 脚本原子归还余量，支持成绩录入后禁止退课
- **时间冲突检测**：基于星期 + 节次 + 周次 + 单双周四维判定，支持选课预检与排课校验
- **学分上限控制**：按学期限制可选总学分
- **缓存预热与同步**：一键预热课程缓存，定时任务每 5 分钟校正数据一致性
- **课程管理**：课程 CRUD、排课管理、状态变更、容量校验
- **成绩管理**：批量录入草稿、发布、撤回；总评计算（平时 30% + 期末 70%）与 4.0 制绩点换算
- **课表查询**：学生课表与教师课表
- **教务管理**：院系、专业、教室、学期、公告的全流程维护
- **学生/教师管理**：账号自动创建、信息维护、启用禁用
- **数据统计**：学生/教师/课程/选课数量统计，热门课程排行，院系分布与成绩分布图表

**基础设施**

- 操作日志注解 `@OperationLog` + AOP 异步落库（记录操作人、耗时、参数、异常）
- 请求耗时拦截器与慢接口告警日志
- 异步线程池配置（拒绝策略为 CallerRunsPolicy，保证日志不丢失）
- Redis 序列化配置（String Key + JSON Value，支持 LocalDateTime）
- Jackson 全局配置（时间格式化、Long 转 String 防精度丢失）
- 选课开关（Redis 动态切换，管理员可随时关闭选课通道）

**前端功能**

- 登录页（含角色演示账号一键填充）
- 首页仪表盘（按角色展示差异化统计与快捷操作）
- 学生端：选课中心（卡片式浏览 + 余量进度条）、我的选课、我的课表（网格课表视图）、我的成绩（成绩单 + 绩点统计）
- 教师端：工作台（授课卡片）、成绩录入（批量填充 + 发布/撤回）、学生名单、教学课表
- 管理端：数据概览（ECharts 图表）、课程管理（含排课编辑）、学生管理、教师管理、基础信息（院系/专业/教室）、学期管理、公告管理、操作日志
- 路由权限守卫（登录校验 + 角色校验）
- 顶栏公告通知、学期标签、个人中心、修改密码

**部署**

- 后端 Dockerfile（多阶段构建 + 健康检查）
- 前端 Dockerfile（Node 构建 + Nginx 托管）
- Nginx 配置（history 回退、静态资源缓存、/api 反向代理、gzip）
- `docker-compose.yml` 一键编排 MySQL + Redis + 后端 + 前端

**文档**

- 中文 README、英文 README
- 架构设计文档、数据库设计文档、API 接口文档、部署运维文档、开发指南

### 已知限制

- **选课模块与成绩模块的 Service 实现尚未编写，待开发者补充**（见下方「未发布」章节）
- 选课余量以 Redis 为准，极端故障场景依赖定时任务与手动同步校正
- 成绩单按学期查询，暂不支持跨学期 GPA 趋势图
- 暂未实现选课排队与消息队列削峰

---

## [未发布]

### 待实现

**选课服务**（`CourseSelectionServiceImpl`）

- ✅ `checkConflict` —— 四维时间冲突判定（星期 + 节次 + 周次 + 单双周），
  算法已提取至 `common/ScheduleConflictUtils` 供排课校验共用
- ✅ `pageMySelections` —— 我的选课记录分页查询
- ✅ `pageCourseStudents` —— 课程学生名单分页查询（含教师课程归属校验）
- ⬜ `selectCourse` —— Redis Lua 原子预占：查重、余量校验、扣减、写入已选集合
- ⬜ `dropCourse` —— 退课并归还 Redis 余量
- ⬜ `pageAvailableCourses` —— 可选课程查询（含实时余量与选课标记）
- ⬜ `preloadCourseCache` —— 课程缓存预热
- ⬜ `syncSelectionCount` —— 选课人数一致性校正

**成绩服务**（`CourseGradeServiceImpl`）

- ✅ `pageGrades` —— 成绩分页查询
- ✅ `listCourseGradeSheet` —— 成绩录入单查询（含教师课程归属校验）
- ✅ `getStudentGradeReport` —— 学生成绩单与绩点统计

**待实现方法**

- ⬜ `pageStudentSelections` —— 查询指定学生的选课记录（管理员视角）
- `pageStudentSelections` —— 查询指定学生的选课记录（管理员视角）
- `adminAddSelection` —— **教务代选**：绕过选课时间窗口，保留学分/冲突/容量等全部校验，记录标记为 `select_type = 2`
- `adminRemoveSelection` —— **教务代退选**：已录入成绩的课程不可退选

> 代选功能的业务背景、校验规则与接口设计详见
> [待实现功能文档 · 2.6 教务代选](./待实现功能.md#26-教务代选管理员)。
> 前端已完整实现（学生管理页「代选课程」），后端实现后即可联调。

**成绩服务**（`CourseGradeServiceImpl` 待编写）

- `inputGrades` —— 批量录入成绩（总评 = 平时 × 30% + 期末 × 70%）
- `publishGrades` / `revokeGrades` —— 成绩发布与撤回
- `listCourseGradeSheet` —— 成绩录入单
- `pageGrades` —— 成绩分页查询
- `getStudentGradeReport` —— 成绩单与绩点统计

**配套事项**

- 4 个 Controller 的注入方式（当前用 `ObjectProvider` 延迟获取，实现后改为直接注入）
- `CourseServiceImpl#getStatistics` 中的成绩分布统计（当前返回空集合）
- `SelectionSyncTask` 定时任务（当前通过 `TASK_ENABLED` 开关默认关闭）

### 现状说明

以下内容已完整就绪，可直接复用：

| 内容 | 说明 |
| --- | --- |
| 实体类 | `CourseSelection`、`CourseGrade`（含展示字段） |
| Mapper 接口与 XML | 全部方法签名与 SQL 已编写 |
| DTO / VO | `GradeInputDTO`、`SelectionResultVO`、`ConflictVO`、`GradeReportVO` |
| Lua 脚本 | `select_course.lua`、`drop_course.lua` 及 `RedisConfig` Bean 注册 |
| 常量与枚举 | `Constants`、`RedisKeys`、`ResultCode`（3001-3012、4001-4004、5006） |
| 占位工具 | `PendingFeature`（统一返回「尚未实现」业务提示） |
| 数据库 | `course_selection`、`course_grade` 表结构及初始化数据 |
| Controller | 完整方法签名、`@Operation` 注解、`@OperationLog` 埋点 |
| 前端 | 全部页面、表单、API 封装、路由与菜单 |
| 算法参考 | `ScheduleConflictTests` 含 7 个冲突判定测试用例 |

### 新增

- 新增文档 [待实现功能.md](./待实现功能.md)：待实现功能清单
  - 选课/成绩模块的实现方法列表与优先级
  - 完整业务规则说明（8 条选课规则 + 8 条成绩规则）
  - 已就绪基础设施清单（Lua 脚本、Redis Key、Mapper 方法、常量）
  - 分阶段实现顺序建议与验收清单

### 待办

- [ ] 实现 `CourseSelectionService`（8 个方法）
- [ ] 实现 `CourseGradeService`（6 个方法）
- [ ] 替换 4 个 Controller 中的占位实现
- [ ] 启用 `SelectionSyncTask` 定时任务
- [ ] 恢复 `CourseServiceImpl` 中的成绩分布统计
- [ ] 补充选课并发与成绩计算的单元测试

---

## 版本规划

### [1.1.0] 计划中

- 选课排队机制（消息队列削峰）
- 成绩单与选课报表 Excel 导出
- 跨学期 GPA 趋势分析图表
- 选课成功、成绩发布的消息通知（邮件 / 站内信）
- 教师端课程评价查看

### [1.2.0] 计划中

- Redis Sentinel / Cluster 高可用支持
- Prometheus + Grafana 监控指标暴露
- 接口限流与防刷策略
- 操作日志可视化管理界面增强
- 移动端适配优化

---

## 版本号说明

| 版本号变更 | 含义 |
| --- | --- |
| 主版本号（1.x.x → 2.x.x） | 不兼容的重大变更 |
| 次版本号（1.0.x → 1.1.x） | 向后兼容的功能新增 |
| 修订号（1.0.0 → 1.0.1） | 向后兼容的问题修复 |

## 变更类型

| 类型 | 说明 |
| --- | --- |
| 新增 Added | 新功能 |
| 变更 Changed | 对现有功能的变更 |
| 弃用 Deprecated | 即将移除的功能 |
| 移除 Removed | 已移除的功能 |
| 修复 Fixed | 问题修复 |
| 安全 Security | 安全相关变更 |
